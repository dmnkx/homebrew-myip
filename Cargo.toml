[package]
name = "myip"
version = "0.1.3"
edition = "2021"
description = "Print local and public IP addresses"
license = "MIT"
repository = "https://github.com/dmnkx/homebrew-myip"
exclude = ["docs/*", "Formula/*", ".cursor/*", ".github/*"]

[dependencies]
ureq = { version = "2.12", default-features = false, features = ["tls"] }
